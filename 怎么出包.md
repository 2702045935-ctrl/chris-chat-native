# CHRIS聊天 · 原生版（云端打包）

这个文件夹就是完整的 iOS 工程，**不需要 Mac**，GitHub 上的苹果电脑会帮你编译。
纯 SwiftUI 原生界面，不是网页壳。

## 一、仓库

已建好：**https://github.com/2702045935-ctrl/chris-chat-native**（Public，公开仓库跑 macOS 机器不花额度）

## 二、出包（2~4 分钟）

1. 改完代码推一次：在这个文件夹开终端，`git add -A && git commit -m "改了什么" && git push`
2. 打开 https://github.com/2702045935-ctrl/chris-chat-native/actions
3. 点最新那条 **build-ios**，等它变成绿色 ✅
4. 页面底部 **Artifacts** → 点 **CHRISChat-unsigned-ipa** 下载
5. 解压得到 `CHRISChat-unsigned.ipa`

## 三、装到 iPhone

1. 打开**爱思助手**，手机用数据线连电脑
2. 左侧 **应用游戏** → 顶部 **导入** → 选那个 `.ipa`
3. 要 Apple ID 就**登录你自己的**（免费账号就行）
4. 手机：设置 → 通用 → VPN与设备管理 → 信任自己的证书

## 四、注意

- 免费 Apple ID 签的 App **7 天**过期，到期再导一次同一个 ipa；聊天记录不会掉（数据在电脑上）。
- App 要连电脑：手机和电脑**同一个 Wi-Fi**，服务器默认 `192.168.2.7:5180`，登录页点「服务器」能改。
- 原生版的 bundle id 是 `com.chris.chatnative`，和旧的网页壳 `com.chris.chat` 不冲突，可以同时装。

## 五、文件说明

| 文件 | 作用 |
| --- | --- |
| `Sources/Core.swift` | 颜色、深色模式、头像、时间格式 |
| `Sources/API.swift` | 数据模型 + 连服务器的所有接口 |
| `Sources/App.swift` | 启动、登录页、全局状态 |
| `Sources/MainTab.swift` | 微信/通讯录/发现/我 四个底栏 |
| `Sources/ChatsView.swift` | 会话列表（左滑：标为未读/不显示/删除） |
| `Sources/ChatDetailView.swift` | 聊天页（气泡、时间条、发送） |
| `Sources/ContactsView.swift` | 通讯录（拼音分组）+ 个人名片 |
| `Sources/DiscoverView.swift` | 发现页 + 朋友圈 |
| `Sources/MeView.swift` | 我 + 设置 + 退出登录 |
| `.github/workflows/build-ios.yml` | 云端编译脚本 |
